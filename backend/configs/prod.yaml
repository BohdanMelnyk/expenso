environment: production

server:
  host: "0.0.0.0"
  port: 8080

database:
  host: localhost
  port: 5432
  username: postgres
  password: password
  database: expenso
  sslmode: require

auth:
  totp_issuer: "Expenso"
  encryption_key: "" # set via AUTH_ENCRYPTION_KEY env var — must be exactly 32 bytes