# Environment Configuration
# Set APP_ENV to switch between config files
# Options: local, prod
APP_ENV=local

# Optional: Override database connection with URL
# DATABASE_URL=postgres://username:password@host:port/database?sslmode=disable